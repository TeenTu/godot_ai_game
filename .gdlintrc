# GDScript lint 配置（gdlint / CI 从仓库根读取本文件）。
#
# 说明：`max-file-lines` 从默认 1000 放宽到 1200，是**有意为之**——
# main_ui.gd / operator_panel.gd 等 Sonar「装配枢纽 / 面板中枢」文件承担了
# 大量 UI 装配逻辑，且声呐综合修复批次（拖曳阵/主动声呐/窄带等）仍持续向其
# 增控件，属正常增长点。放宽仅惠及超过 1000 行的枢纽文件；
# 其余游戏文件仍以 1000 行为约束（当前均远低于该值）。
# 若单个文件因职责膨胀再超 1200，应优先「拆分出子面板/子控制器」而非继续调高本值。
max-file-lines: 1200
